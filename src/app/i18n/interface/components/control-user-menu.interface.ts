export interface ControlUserMenuMessagesInterface {
  userProfile: string;
  accountSettings: string;
  avatarSettings: string;
  appSettings: string;
  logout: string;
  login: string;
  register: string;
  ariaLabel: {};
}
