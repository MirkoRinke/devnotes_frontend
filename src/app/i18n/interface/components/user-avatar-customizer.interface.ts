export interface UserAvatarCustomizerMessagesInterface {
  heading: string;
  subheading: string;
  reset: string;
  save: string;
  ariaLabel: {
    prev: string;
    next: string;
    randomize: string;
    reset: string;
    save: string;
  };
}
