import { PrismLanguagePipe } from './prism-language-pipe';

describe('PrismLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new PrismLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
