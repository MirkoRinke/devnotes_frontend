import { EscapeCloseDirective } from './escape-close.directive';

describe('EscapeCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new EscapeCloseDirective();
    expect(directive).toBeTruthy();
  });
});
