import { AccessibleButtonDirective } from './accessible-button-directive';

describe('AccessibleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessibleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
